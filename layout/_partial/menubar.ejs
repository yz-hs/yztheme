<nav class="navbar navbar-default navbar-inverse navbar-fixed-top margin-0 menubar sticky-it" role="navigation"> 
    <div> 
      <a href="/" class="navbar-brand text-lt">
          <span class="glyphicon glyphicon-home"></span> <span class="menubar-title"><%= config.title %></span>
      </a>
	</div>
	<form class="navbar-form navbar-left menubar-search" role="search">
      <div class="form-group">
        <input type="text" class="form-control" placeholder="输入关键词搜索…">
        <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-search"></span></button>
      </div>
    </form>
    <div class="btn-group menubar-button navbar-btn">
	  <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown"> 链接 <span class="caret"></span></button>
	  <ul class="dropdown-menu" role="menu">
		<% for (name in theme.menu) { %>
          <% if (url_for(page.path) == url_for(theme.menu[name])) { %>
            <li class="active">
          <% } else { %>
            <li>
          <% } %>
              <a href="<%- url_for(theme.menu[name]) %>" class="menu-item-link"><%- __('Menu.' + name) %></a>
            </li>
        <% } %>
	  </ul>
    </div>
</nav>